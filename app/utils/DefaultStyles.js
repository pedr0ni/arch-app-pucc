import { StyleSheet } from 'react-native';

export const Colors = {
    defaultBlue: '#16336c'
};

export const Styles = StyleSheet.create({
    defaultShadow: {
        shadowColor: "#000",
        shadowOffset: {
            width: 0,
            height: 2,
        },
        shadowOpacity: 0.25,
        shadowRadius: 3.84,

        elevation: 5
    }
});